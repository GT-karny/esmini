{
  "native_reference": {
    "enabled_for_golden_creation": true,
    "used_for_daily_gate": false,
    "notes": [
      "Native controller outputs are used only to bootstrap expected ranges and sanity checks.",
      "RealDriver pass/fail is based on feature KPIs and approved golden data."
    ]
  },
  "reference_capture": {
    "command_template": "esmini --osc {scenario} --headless --fixed_timestep 0.05 --record native_sim.dat",
    "artifacts": ["native_log.txt", "native_sim.dat", "native_sim.csv"]
  }
}
