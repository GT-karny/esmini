manifest: resources/xosc/verification/scenario_handoff_batch.yaml
note: Committed regression baseline for the VirtualDriver behavioral batch. Compared per-scenario/per-matcher
  by scripts/check_regression_baseline.py. Any deviation (new fail OR a known fail turning pass) fails
  the check. Refresh ONLY after an intentional behavior change, with --update, and review the diff before
  committing.
expected_summary:
  pass: 3
  fail: 0
  needs-review: 0
  error: 0
scenarios:
  scenario_deactivate_vd:
    status: pass
    matchers:
    - event: vd_control_relinquished
      status: pass
  scenario_domain_takeover_vd:
    status: pass
    matchers:
    - event: vd_control_relinquished
      status: pass
  scenario_split_domain_md_vd:
    status: pass
    matchers:
    - event: domain_split_holds
      status: pass
